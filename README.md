# cms_migration
Websites URLs to Json

see a folder structure like this:

--folder-backend
        -extractor.py
--folder-cms_env
--folder-models
--folder-pages

        -app.py
        -requirements.txt
        -urls.txt
        -cms_arc.png
        -cms-flow.png

